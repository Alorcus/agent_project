"""Consolidation: reflection over fragments, folding extracted claims into
higher-level ones (§ 2.6). Exists in stage 0 only so the I-6 import lint has
a file to check; the implementation lands in stage 5."""
